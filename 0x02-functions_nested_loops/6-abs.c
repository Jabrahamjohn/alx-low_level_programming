#include "holberton.h"

/**
 * _abs - absolute value of an integer
 *@r: used as a variable to define absolute value
 * Return: absolute value
 */
int _abs(int r)
{
	if (r >= 0)
{
	return (r);
}
	else
{
	return (r * -1);
}
}
