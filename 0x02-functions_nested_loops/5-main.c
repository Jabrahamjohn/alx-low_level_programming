#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	int n = -5;
	int sign = print_sign(n);
	printf("%d\n", sign);
	return 0;
	}
