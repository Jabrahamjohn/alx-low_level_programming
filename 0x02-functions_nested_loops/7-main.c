#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	int r = -12345;
	int last = print_last_digit(r)
	putchar(last);
	return (0);
}
